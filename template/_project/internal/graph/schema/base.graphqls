# Root schema. Resource fragments (one per resource) `extend` these types.
# gqlgen merges every *.graphqls file in this directory.
type Query {
  _health: String!
}

type Mutation {
  _noop: Boolean!
}
